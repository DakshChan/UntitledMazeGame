import java.awt.Rectangle;

public class CollisionMap {

	CollisionMap(float[][] maze) {
		int HEIGHT = maze.length;
		int WIDTH = maze[0].length;

		for (int i = 0; i < HEIGHT; i++) {
			for (int j = 0; j < WIDTH; j++) {
				if (maze[i][j] == 0.0f) {

				}
			}
		}

	}

}
